[package]
name = "rustywallet-bloom"
version = "0.2.0"
edition = "2021"
authors = ["RustyWallet Contributors"]
description = "Fast and memory-efficient Bloom Filter with counting support for rustywallet"
license = "MIT"
repository = "https://github.com/rustywallet/rustywallet"
keywords = ["bloom-filter", "probabilistic", "data-structure", "counting-bloom"]
categories = ["data-structures", "algorithms"]

[dependencies]
# No external dependencies - pure Rust implementation

[dev-dependencies]
proptest = "1.0"
