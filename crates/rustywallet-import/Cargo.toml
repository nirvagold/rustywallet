[package]
name = "rustywallet-import"
version = "0.2.0"
edition = "2021"
authors = ["NirvaGold"]
description = "Import private keys from various wallet formats (WIF, hex, mnemonic, BIP38, mini key, descriptors)"
license = "MIT"
repository = "https://github.com/nirvagold/rustywallet"
documentation = "https://docs.rs/rustywallet-import"
keywords = ["bitcoin", "wallet", "import", "cryptocurrency", "descriptor"]
categories = ["cryptography::cryptocurrencies"]
readme = "README.md"

[dependencies]
# Internal crates
rustywallet-keys = { version = "0.1", path = "../rustywallet-keys" }
rustywallet-mnemonic = { version = "0.2", path = "../rustywallet-mnemonic" }
rustywallet-hd = { version = "0.3", path = "../rustywallet-hd" }
rustywallet-address = { version = "0.3", path = "../rustywallet-address" }
rustywallet-descriptor = { version = "0.2", path = "../rustywallet-descriptor" }

# Crypto
sha2 = "0.10"
aes = "0.8"
scrypt = "0.11"

# Encoding
bs58 = "0.5"
hex = "0.4"

# Serialization
serde_json = "1.0"

# Error handling
thiserror = "2.0"

# Utilities
zeroize = { version = "1.8", features = ["derive"] }

[dev-dependencies]
proptest = "1.0"

[features]
default = []
